# OBS EVENTS — PROGRESS

Current phase: 2 — IN PROGRESS (checkout, payments, ticketing)
Last session: 2026-07-09 — Phase 2 started. Tasks 2.1 (21/21) + 2.2 (34/34) + 2.3 Razorpay (19/19) + 2.4 Stripe (13/13) + 2.5 fulfilment (12/12) + 2.6 checkout+booking (6/6) + 2.7 my-tickets/orders (8/8) COMPLETE ✅; only 2.8 (validation page) left. Phases 0–1 complete before this (Phase 1 EXIT 17/17). Demo organizer: demo.organizer@obs.events / Organizer@123 (APPROVED) + 3 seeded published events.
Env note: SERVICE_FEE_PERCENT=5, ORDER_HOLD_MINUTES=15 set; **gateway keys (Razorpay/Stripe) + AWS creds are EMPTY**, SMTP (Gmail) is set. So webhook fulfilment is verified via locally-signed payloads; live gateway create/dashboard-webhooks + real S3 objects are the Phase-2 EXIT human sign-off. Verify scripts run with `SMTP_HOST=` to force jsonTransport (no real sends).
Stack: MERN (MongoDB Atlas + Mongoose · Express · React 18 + Vite · Node 20) — see obs-events-build-plan.md v1.1

## Phase 0 — Foundation
- [x] 0.1 Repo scaffold: client/ (Vite + React 18 + Tailwind + React Router v6), server/ (Express + Mongoose, ES modules, nodemon), root npm workspaces
- [x] 0.2 Mongoose models (§5); Atlas or local single-node replica set; seed.js: admin, 12 categories, 108 chapters (type, tier, pillarGroup, ecosystemTier A–E, isFlagship, sortOrder — Appendix A), CMS stubs
- [x] 0.3 Auth: register/login (bcrypt 12), refresh rotation, Google id_token verify, forgot/reset, middleware (requireAuth, requireRole, zod validate, rate limits), error handler
- [x] 0.4 Utils: S3 presigned PUT/GET, mailer (Nodemailer SMTP, provider-agnostic, + EmailLog), nextSeq() counters, slugify
- [x] 0.5 Client base: layout + navbar/footer, auth pages, role guards in layouts, axios client with silent-refresh interceptor
- [x] EXIT: email + Google signup verified end to end; refresh rotation works; mongosh shows 108 chapters + 12 categories + admin — ✅ signed off 2026-07-08 (user confirmed browser sign-in for email + Google)

## Phase 1 — Public catalog & event lifecycle
- [x] 1.1 Organizer apply → admin approve/reject + emails
- [x] 1.2 Events CRUD in DRAFT, slug gen, banner presigned upload, ownership guards
- [x] 1.3 Wizard UI (6 steps) saving drafts per step; step 3 Google Places Autocomplete → address/lat/lng/placeId (§8.7) + /geo/geocode fallback
- [x] 1.4 Submit → PENDING_APPROVAL; admin queue approve/reject (+reason) + emails; state machine §6 in service layer
- [x] 1.5 Public listing API (all filters + indexes); listing page with filter rail, cards, pagination, sort
- [x] 1.6 Event details (booking card disabled) incl. embedded Google Map + directions on venue block, organizer profile, chapters directory (grouped by type/tier, flagship row) + chapter page (join/leave), home, search, share, viewsCount, react-helmet-async meta/OG
- [x] EXIT: demo organizer takes an event draft → approval → public listing; browse/filter/chapters work logged out — ✅ VERIFIED 17/17 (2026-07-08). Full browser click-through is the human sign-off (backend + client-build all verified; run `npm run dev --workspace server` + `npm run dev --workspace client`).

## Phase 2 — Checkout, payments, ticketing
- [x] 2.1 TicketTypes + PromoCodes CRUD (wizard steps 4–5 live)
- [x] 2.2 POST /orders atomic reserve ($expr guard in Mongo txn, §8.1) → PENDING/held draft order; order+payment state model §8.0; expiry cron + cancel
- [x] 2.3 Razorpay order/verify/webhook (test mode)
- [x] 2.4 Stripe intent/webhook (test mode)
- [x] 2.5 Fulfilment §8.3: tickets, QR, PDF template, invoice, S3, emails
- [x] 2.6 Checkout page (countdown, gateway selector) + success page + live booking card on event details
- [x] 2.7 My tickets, ticket detail (QR, PDF, .ics), order history
- [x] 2.8 Public validation page /t/:token
- [ ] EXIT: paid (both gateways), free, and promo flows produce tickets; PDFs in S3 + inbox; expiry restores inventory

## Phase 3 — Organizer & admin operations
- [ ] 3.1 Organizer dashboard KPIs
- [ ] 3.2 Registrations table + XLSX export (exceljs)
- [ ] 3.3 Check-in endpoint §8.4 + scanner page (html5-qrcode) + stats
- [ ] 3.4 Refund request → admin queue → gateway refund → webhook completion §8.5 + inventory restore + emails
- [ ] 3.5 Admin: users, organizers, events moderation (+feature toggle), transactions, categories CRUD, chapters CRUD (all hierarchy fields), CMS CRUD + public render, AuditLog on mutations
- [ ] EXIT: full dry run — create → approve → sell (test) → export → check in → refund one order

## Phase 4 — Reports, automation, launch
- [ ] 4.1 Reports aggregations §11 + admin reports page (recharts)
- [ ] 4.2 remind24h + completeEvents crons
- [ ] 4.3 Hardening: zod everywhere, helmet, CORS allowlist, webhook raw-body ordering, private S3 + signed reads
- [ ] 4.4 SPA SEO: server /sitemap.xml + robots, helmet meta/OG per event; branding pass (white + red #F84464 — obs-home-ui-prompt.md)
- [ ] 4.5 Deploy: Atlas (M0 dev / M10 prod), EC2 API (pm2 + nginx + certbot), React build on S3 + CloudFront, live webhook URLs, live-keys checklist
- [ ] EXIT: production URL serves a full booking flow

## Phase 5 — Community, content & programs (post-MVP · UI in obs-frontend-new-sections-ui-prompt.md)
- [ ] 5.1 Open chapter creation: POST /chapters (any user, isOfficial=false), PATCH (creator edits), GET /me/chapters, admin status/official/flagship; /chapters/create + /account/chapters + Community chapters section. Events stay organizer-gated.
- [ ] 5.2 Speakers: model + admin CRUD, /speakers + /speakers/:slug, organizer attaches speakerIds, speakers block + home rail
- [ ] 5.3 Sponsors & partners: Sponsor model (tiers/scope) + admin CRUD, /sponsors showcase + what-we-offer, event sponsors block, PartnerApplication + /become-a-sponsor → admin queue
- [ ] 5.4 Media: Article model + admin CRUD, /news + /news/:slug, newsroom rail
- [ ] 5.5 100 Days Program: Program + ProgramDay models, seed current edition (15 Oct→22 Jan, 100 days), /program overview (season status + country filter + day-by-day) + /program/day/:n, link events via programId/programDayNumber, home banner; repeats yearly
- [ ] 5.6 Ownership + Launchpad: event ownership OBS/Partner + All/OBS/Partner tabs (?owner); isLaunch/launchAt + /launches with countdowns + home rail
- [ ] 5.7 Frontend polish: AppLoader (initial) + RouteProgress bar + chapter-highlight hero band
- [ ] EXIT: user creates a community chapter; speaker/sponsor/article render; /program day-by-day works; owner tabs filter; /launches countdown

## Decisions
- Phase 2 task 2.8 (2026-07-09): **Public validation page**. `GET /api/v1/tickets/validate/:qrToken` (no auth, `modules/tickets/tickets.public.routes.js` mounted at `/api/v1/tickets`) → `{ eventTitle, startAt, venue, attendeeName (masked "B••••• K"), status, checkedInAt, ticketNumber }`; 404 TICKET_NOT_FOUND for unknown tokens. **Read-only** — actual check-in is the organizer scanner (Phase 3 §8.4). Client `Validate` page rewritten to fetch by token and show VALID (green ✓) / USED (grey clock + time) / CANCELLED|REFUNDED (red ✗) / not-found; route reconciled `/t/:status` → **`/t/:token`** (the cosmetic mismatch noted in Phase 1).
- Phase 2 task 2.7 (2026-07-09): **My tickets / ticket detail / orders**. `modules/tickets`: `GET /me/tickets?scope=upcoming|past` (VALID+USED, joined event + ticketType name, scope by `event.endAt` vs now), `GET /me/tickets/:id` (owner; returns **server-generated `qrDataUrl`** (data:image/png) + `validationUrl` so the browser needs no QR lib), `GET /me/tickets/:id/pdf` (owner). **PDF endpoint STREAMS a freshly-built PDF** (Content-Type application/pdf) rather than a signed S3 URL — works with or without S3 (dev has no creds); the client fetches it as a blob (auth header) and triggers a download. Ticket detail QR is an `<img src={qrDataUrl}>`. **Add to calendar** = client-side `.ics` (`lib/ics.js`, all-UTC). Rewrote `MyTickets` (upcoming/past tabs), `TicketDetail` (real QR + PDF blob + .ics), `Orders` (real order history; PENDING row → resume `/checkout/:id`, PAID → `/checkout/:id/success`). All account pages behind the existing RequireAuth.
- Phase 2 task 2.6 (2026-07-09): **Checkout + live booking card**. Public `GET /events/:slug` now returns **active `ticketTypes[]`** (each with `onSale` folding active+sale-window+availability; inactive types excluded entirely) + `serviceFeePercent` for the card's fee estimate. **BookingCard** (event details, replaces the disabled card) — steppers honoring min/max + availability, promo input, fee-inclusive estimate; "Book now" → `createOrder` → **free order (PAID) routes to /checkout/:id/success, else /checkout/:id**; unauthenticated → auth modal. **Checkout page `/checkout/:orderId`** (RequireAuth): loads `/me/orders/:id`, **live countdown from `expiresAt`**, redirects PAID→success, shows expired/cancelled states, gateway selector (Razorpay+Stripe for INR, Stripe-only otherwise). **Razorpay** = checkout.js modal (script via `lib/loadScript`) → handler calls verify (fast-path) → success. **Stripe** = `stripeIntent` → Payment Element (`@stripe/stripe-js`, `StripePaymentForm`) → `confirmPayment`. Both catch the 503 not-configured and toast. **Success `/checkout/:orderId/success`** polls `/me/orders/:id` until PAID (webhook-driven), then View tickets / Download invoice (if `invoice.pdfUrl`) / My orders. **Verified headlessly**: the data contract (ticketTypes/fee on detail; order shape for checkout; free→PAID) 6/6 + client build; **the gateway pay modals need real keys + a browser → Phase-2 EXIT sign-off**. Dep: @stripe/stripe-js.
- Phase 2 task 2.5 (2026-07-09): **Fulfilment (§8.3)** enriches the shared `markPaidAndFulfil`: per ticket → QR PNG (`utils/qr.js`, content `${APP_URL}/t/${qrToken}`) + a ticket PDF (`utils/pdf.js` pdfkit — wordmark, event, date in event tz, venue/online, attendee, type, number, 140px QR, T&C) → S3 `tickets/{eventId}/{ticketNumber}.pdf` (best-effort) + email attachment; invoice PDF (paid only) numbered `OBS-INV-{YYYY}-{seq}` via `nextSeq('invoice')` → S3 `invoices/{orderNumber}.pdf` + embedded `order.invoice`. Emails: free → REGISTRATION_CONFIRMATION (tickets attached), paid → PAYMENT_SUCCESS (invoice attached) + TICKET_DELIVERY (tickets attached); **>8 MB of attachments → links instead** (§8.3.5). `utils/mailer.sendMail` now forwards `attachments`. **S3 is best-effort + gated by `isS3Configured()` (explicit keys OR prod-IAM)** — in dev without creds uploads are skipped (avoids an IMDS hang) and `pdfUrl` stays null while PDFs still attach to emails, so tickets are usable; **real S3 objects + inbox delivery are the EXIT sign-off** (needs AWS creds; SMTP is live). Deps: qrcode, pdfkit. Client ticket display = 2.7.
- Phase 2 task 2.4 (2026-07-09): **Stripe** mirrors Razorpay. `POST /payments/stripe/intent` (requireAuth) guards the held order, creates a PaymentIntent (`amount` in smallest unit = our paise; `metadata.orderId`; `automatic_payment_methods`), sets `order.gateway=STRIPE`, records Payment(CREATED); 503 when unconfigured. Stripe handles **both INR (toggle) and non-INR (only option)** — no currency restriction (unlike Razorpay). `POST /webhooks/stripe` (raw body, mounted with the Razorpay one before json) verifies via `stripe.webhooks.constructEvent(raw, sig, WEBHOOK_SECRET)` (pure crypto, no network → works with only the webhook secret; `config/stripe.js` builds the client with a placeholder key when no secret key is set); links via `intent.metadata.orderId`; **payment_intent.succeeded** → idempotency + amount guard + Payment CAPTURED + `markPaidAndFulfil`; **payment_intent.payment_failed** → FAILED + release. Verified headlessly with the SDK's `generateTestHeaderString` + a dummy webhook secret; **live intent creation needs a real secret key (EXIT sign-off)**. `stripe` dep added.
- Phase 2 task 2.3 (2026-07-09): **Razorpay** in `modules/payments`. Webhook raw-body ordering: `app.use('/api/v1/webhooks', express.raw({type:'*/*'}), webhookRoutes)` mounts **before** `express.json()` (and before the global limiter — gateways retry). `POST /payments/razorpay/order` (requireAuth) guards the held order (owner/PENDING/not-expired/not-free/INR-only), sets `order.gateway=RAZORPAY`, creates a Razorpay order + a Payment(CREATED) row, returns `{razorpayOrderId, amount(paise), keyId}`; 503 when unconfigured. `POST /payments/razorpay/verify` checks `HMAC_SHA256(order_id|payment_id, KEY_SECRET)` (timing-safe) and **records the attempt but never fulfils** (webhook does). `POST /webhooks/razorpay` verifies `HMAC_SHA256(rawBody, WEBHOOK_SECRET)` (400 on mismatch), links payment→order via `Payment.gatewayOrderId`, then **payment.captured** → idempotency (already-PAID → 200), **amount-mismatch guard** (ignore, never fulfil under/overpayment), Payment CAPTURED + `markPaidAndFulfil` (the conditional flip gates single fulfilment); **payment.failed** → Payment FAILED + `releaseHeldOrder(FAILED)`. Verified headlessly with dummy secrets + locally-signed payloads; **live order creation + real dashboard webhooks need the user's test keys (EXIT sign-off)**. Client checkout wiring is 2.6. `razorpay` dep added.
- Phase 2 task 2.2 (2026-07-09): **`POST /orders` (§8.1)** validates event PUBLISHED+future + each ticket type active/in-window/qty within min–max, **recomputes all money server-side** (client sends only `{ticketTypeId, quantity}` + optional `promoCode` — no prices to spoof), reserves inventory in **one `withTransaction`** with the `$expr` `$lte[$add[$quantitySold,qty],$quantityTotal]` guard (`modifiedCount!==1` → abort → **409 SOLD_OUT**; multi-item aborts roll back earlier increments), then inserts the **PENDING** order (embedded item snapshots + `expiresAt = now + ORDER_HOLD_MINUTES`). `orderNumber` from `nextSeq('order')` is generated **before** the txn (a retry/abort only skips a number). **Gateway at creation** = `FREE` if total 0, else `RAZORPAY` (INR) / `STRIPE` (non-INR) as a default; the chosen gateway is set when payment is initiated (2.3/2.4). **Free order (total 0, incl. a 100%-off promo)** → `markPaidAndFulfil` flips PENDING→PAID + creates tickets + REGISTRATION_CONFIRMATION, no gateway. **`releaseHeldOrder(orderId, toStatus)`** re-checks PENDING inside a txn then flips + decrements `quantitySold` — shared by cancel (`POST /orders/:id/cancel`, owner + PENDING-only), the expiry cron, and (2.3/2.4) the failed-payment webhook. **Expiry cron** `expireOrders` (node-cron `*/5 * * * *`, started in index.js only) → PENDING past `expiresAt` → EXPIRED + restore; idempotent (conditional flip). `GET /me/orders`(+`/:id`) list/detail with ownership. Fee = `round((subtotal−discount)×SERVICE_FEE_PERCENT/100)`; SERVICE_FEE_PERCENT=5, ORDER_HOLD_MINUTES=15 from env. node-cron added.
- Phase 2 sequencing (2026-07-09): **fulfilment is ONE shared `fulfillOrder()`** used by the free-order path (2.2) and both webhooks (2.3/2.4). Since it's needed before task 2.5, its **core** (create tickets + mark PAID + email) is built in 2.2 and **enriched** (QR PNG, ticket/invoice PDFs, S3 upload best-effort, PDF email attachments) in 2.5 — one function, incremental, no rework. Idempotency = the atomic conditional flip `Order.updateOne({_id, status:'PENDING'},{status:'PAID'...})`; only the winning flip fulfils (webhook re-delivery → already PAID → 200, no double fulfil). The embedded `order.invoice` subdoc IS the "Invoice row" (§8.3) — no separate collection. **Verification gating**: atomic reserve/SOLD_OUT/money/promo/free/expiry + webhook signature+idempotency+fulfilment are all verified headlessly (Razorpay HMAC + Stripe `generateTestHeaderString`, no network); **live gateway order/intent creation, real test-mode dashboard webhooks, real S3 objects, and inbox delivery need the user's test keys + AWS creds → Phase-2 EXIT human sign-off** (same pattern as Phase 0/1 browser/Google). PDFs are also attached to emails from in-memory buffers so tickets are usable even without real S3.
- Phase 2 task 2.1 (2026-07-09): **TicketTypes + PromoCodes CRUD** nested under `/api/v1/organizer/events/:eventId/{ticket-types,promo-codes}` (mergeParams routers, mounted before `/organizer/events` so the more-specific prefix wins). Ownership reuses the exported `loadOwnedEvent(organizerId, eventId)`. **All money is integer paise on the wire** (`price`, flat `discountValue`, `minOrderAmount`); the wizard editors take ₹ and convert ×100. Rules enforced in the service on the merged doc: ticket `maxPerOrder≥minPerOrder`, sale-window order, `quantityTotal≥quantitySold` (400s); delete a ticket type only when `quantitySold===0` (else 409 TICKET_TYPE_HAS_SALES). Promo `code` uppercased + unique per event (dup → 409 PROMO_CODE_EXISTS), PERCENT 1–100 / FLAT ≥1 paise (else 400 INVALID_DISCOUNT); delete only when `usedCount===0` (else 409 PROMO_CODE_USED). No event-status gate on ticket/promo CRUD (organizers manage inventory pre- and post-publish). Client: `TicketTypesEditor` + `PromoCodesEditor` components make wizard steps 4–5 live (add/edit/delete inline); they load only when the draft event exists (step 1 created it).
- Phase 1 task 1.6 (2026-07-08): **Public detail endpoints** — `GET /events/:slug` (PUBLISHED|COMPLETED only; **increments viewsCount** best-effort; **meetingLink stays hidden** — revealed to ticket holders in Phase 2), `GET /events/:slug/similar` (same category/chapter, next 4 upcoming), `GET /organizers/:slug` (public profile + upcoming events), `GET /chapters/:slug` (detail + memberCount + upcoming events + `isMember` when signed in), `POST|DELETE /chapters/:id/join` (requireAuth, idempotent). New `optionalAuth` middleware powers isMember. New public routes mounted at `/api/v1/organizers` (plural; the authed self-service stays at `/api/v1/organizer`). **Embedded map** uses the keyless `maps.google.com/maps?q=lat,lng&output=embed` iframe (renders a real pinned map without a browser key, which is empty here) + a §8.7 "Get directions" link; when a browser key is later set, the Maps Embed API can swap in. **Booking card is rendered-but-DISABLED** (Registration panel + disabled Book-now) per the Phase-1/2 boundary. **Share** = WhatsApp/X/LinkedIn/copy. **react-helmet-async** added (`HelmetProvider` in main.jsx) + a reusable `Seo` component on every public page (title + description + OG/Twitter). **Home rewritten to real data** (real hero band + category chips + Happening-soon/Recently-added rails + real chapter spotlight); the mock hero carousel, CategoryTiles, ChapterHighlightBand and the Phase-5 rails (speakers/sponsors/news/program/launches) were dropped from the Phase-1 home to avoid dead links — they return with real data in Phase 5. Search wired to `GET /events?q` + client-side chapter match (organizer search dropped — no endpoint). Chapters index groups the 108 into flagship / countries-by-tier / cities / thematic; the "Create a chapter" button was removed (open creation is Phase 5). **Seed** now creates a demo organizer + 3 published events (idempotent; dates refresh to near-future each reseed) so the public site isn't empty for the demo.
- Phase 1 task 1.5 (2026-07-08): **Public `GET /events`** returns PUBLISHED-only, upcoming (`endAt >= now`) unless `?includePast=1`; filters q (regex over title/description/city/venueName — chose regex over the `$text` index for partial matching; the text index stays available), city (exact, case-insensitive), category & chapter (by **slug** → resolved to id; unknown slug → empty page), mode (online/venue), dateFrom/dateTo (on startAt), sort soonest|newest|popular; page/limit (default 12). All filter fields are indexed (status+startAt, city, categoryId, chapterId). `price`/`owner`/`program` params are accepted but **ignored** in Phase 1 (Phase 2/5). **Listing page rebuilt on the API**: dropped the mock **owner tabs** (Phase 5) and **price filter** (Phase 2 — events have no price until ticket types); category/chapter are **single-select** (matches the singular API params, was multi in the mock); date presets (Today/Tomorrow/Weekend/Month) compute dateFrom/dateTo client-side; server-side sort + **load-more** pagination. New `components/common/ApiEventCard.jsx` (real-data 2:3 poster; no fake likes/promoted like the mock `EventCard`, which also broke on string ids); the mock `EventCard`/`EventGrid`/`data/events.js` stay for not-yet-wired pages. Navbar city-selector → listing `?city` wiring is a minor follow-up (listing already honors a `city` param).
- Phase 1 task 1.4 (2026-07-08): **State machine enforced in the service layer** (§6). `POST /organizer/events/:id/submit` = DRAFT → PENDING_APPROVAL, but only after `assertSubmittable` passes (category, description ≥10 chars, startAt+endAt, endAt>startAt, startAt in the future, and meetingLink if online / venueName+address if in-person) — else **422 EVENT_INCOMPLETE** with `details.missing[]` so the UI can list what's needed. Non-DRAFT submit → 409 INVALID_EVENT_STATE. Admin: `GET /admin/events?status`, `POST /admin/events/:id/approve` (PENDING_APPROVAL → PUBLISHED + publishedAt, clears rejectionReason) and `/reject` (**reason required**, → REJECTED + rejectionReason); both 409 on non-pending, write AuditLog (EVENT_APPROVED/REJECTED), and email the organizer (EVENT_APPROVED links to the public page, EVENT_REJECTED links to the edit page). REJECTED→DRAFT still happens on organizer edit (1.2). The feature toggle (`PATCH /admin/events/:id {isFeatured}`) is deferred to Phase 3.5. Client: wizard step 6 "Submit for approval" (auto-transitions a REJECTED event back to DRAFT first, surfaces the 422 missing-fields toast); admin `/admin/events` queue (status tabs + approve/reject-with-reason) added to the AdminLayout nav.
- Phase 1 task 1.3 (2026-07-08): **Wizard scope for Phase 1** — steps 1 (Basics), 2 (Banner), 3 (Venue & schedule) are functional and save a draft per step (step 1 `POST /organizer/events` creates → holds eventId in component state; later steps `PATCH`); steps 4 (Tickets) + 5 (Promos) render as locked "Coming in Phase 2" placeholders (they go live in 2.1); step 6 is a read-only Review whose CTA is "Done" (saves already happened) — the actual **Submit for approval** action is task 1.4. **Google Places (§8.7)**: `lib/googleMaps.js` loads Places only when `VITE_GOOGLE_MAPS_API_KEY` is set (it's currently EMPTY in this env), so the wizard degrades to manual address entry + the **server `/geo/geocode` fallback**, which works because the **server** `GOOGLE_MAPS_API_KEY` is set (verified live — 'Jio World Convention Centre, Mumbai' → 19.06,72.87 + city/country/placeId). Built backend deps the wizard needs, formally §7 catalog endpoints scheduled for 1.5/1.6 but required now: **GET /categories**, **GET /chapters** (flat list + ?type&tier), **POST /geo/geocode** (requireAuth). **OrganizerLayout gates on the APPROVED profile via /organizer/me** (not token role) so a just-approved organizer isn't locked out; `/organizer/apply` stays outside the layout. Datetime: the wizard treats `datetime-local` as browser-local wall time → ISO (`new Date(v).toISOString()`); precise per-event-timezone handling is deferred (event.timezone default Asia/Kolkata). Banner: client presigns → PUTs to S3 → PATCHes `bannerUrl`. Header account menu now shows role-aware "Admin panel" / "Organizer portal" links. Minimal organizer Events list built (needed to create/resume/delete drafts); full dashboard KPIs remain 3.1.
- Phase 1 task 1.2 (2026-07-08): **Event model relaxed for draft-first** — the wizard (§10) saves a draft per step, so `categoryId`, `description`, `startAt`, `endAt` are no longer `required` at the model level (only `title` + `slug` are); completeness is enforced at SUBMIT (task 1.4) in the service. `POST /organizer/events` therefore needs only a `title`. **Ownership + state guards live in the service**: `loadOwnedEvent` throws 404 (missing) / 403 NOT_EVENT_OWNER (not owner); edit/delete allowed only in DRAFT|REJECTED (else 409 EVENT_NOT_EDITABLE / EVENT_NOT_DELETABLE, delete DRAFT/REJECTED-only per §7); editing a REJECTED event transitions it back to DRAFT and clears `rejectionReason` (§6). **`requireApprovedOrganizer` gates on an APPROVED OrganizerProfile (DB-authoritative), not the token role** — reconciles §4's "check both" without locking out a just-approved organizer whose token still says USER (profile APPROVED is strictly stronger than a bare role and is what grants ORGANIZER). **Banner presign flow**: `POST /organizer/events/:id/banner {contentType}` returns `{uploadUrl, key, fileUrl}` (key `banners/<eventId>/<uuid>.<ext>`, image types only); client uploads directly to S3 then persists `bannerUrl` via PATCH. Public banner serving (bucket policy / CloudFront) is deferred to Phase 4 hardening (`utils/s3.js` `objectUrl()` builds the canonical URL). Events module split: `events.service/controller/schemas` + `events.organizer.routes.js` (mounted `/api/v1/organizer/events`); the public catalog routes (`events.public.routes.js`) land in 1.5. Client wiring of the wizard/list against these endpoints is task 1.3 (1.2 is backend-only).
- Phase 1 task 1.1 (2026-07-08): **Role promotion timing** — a signed-in USER applies via `POST /organizer/apply` which creates a PENDING `OrganizerProfile` and leaves `user.role = USER`; the role is promoted to ORGANIZER only on admin **approval** (so role = granted capability, and the §4 dual check `role===ORGANIZER && profile APPROVED` still blocks SUSPENDED/REJECTED). Consequence: an approved organizer's *current* access token still says USER until their next refresh/login — realistic since they log in after the approval email. Promotion uses a conditional `User.updateOne({_id, role:'USER'}, {role:'ORGANIZER'})` so an ADMIN is never demoted (and it doesn't depend on the `.populate('userId','name email')` projection, which omits `role` — that projection caused the only E2E failure, now fixed). **Re-apply**: a REJECTED applicant can re-apply — the same unique-per-user row is reset to PENDING (can't create a 2nd; `userId` is unique). **Organizer rejection reason is optional** (emailed if given; there's no `rejectionReason` field on OrganizerProfile — event rejection in 1.4 will require one per §6 and Event has the field). Reason is captured in the AuditLog meta. **Every admin mutation writes an AuditLog** (new `utils/audit.js`, best-effort — a logging failure never breaks the action).
- Phase 1 task 1.1 (2026-07-08): **Client portal was non-functional scaffolding** — the ~17 `pages/organizer/*` + `pages/admin/*` files import `components/portal/Kit` (never existed) and call `api.*` domain methods absent from the axios client, and none were routed in App.jsx. Built the shared **`components/portal/Kit.jsx`** once (Btn/Card/PageHead/Pill+statusTone/Table/Tabs/Loading/EmptyState/SearchInput/Field/StatCard/StatGrid/formatPrice — presentational infra, not a feature) + **`PortalShell` + `AdminLayout`** (sidebar chrome, nav grows per task). Client `api.js`: domain methods are **attached to the axios instance** (`api.applyOrganizer`, `api.myOrganizerProfile`, `api.adminOrganizers`, `api.approveOrganizer`, `api.rejectOrganizer`) and resolve to the **unwrapped payload** (`.data.organizer(s)`), matching how the scaffold pages already call them; added `apiErrorCode()` for branching on typed codes. **Kit.formatPrice takes integer paise** (÷100) — distinct from `data/events.js` `formatPrice` (whole rupees, mock) used by public pages. Only the 1.1 pages were wired (`/organizer/apply` behind RequireAuth; `/admin` + `/admin/organizers` behind RequireRole ADMIN); the other portal scaffolds stay dormant (unrouted → not bundled) until their task. `/list-your-event` (mock marketing page) CTAs now navigate to the real `/organizer/apply`. Following existing client palette (**brand = gold `#C99E25`**, `brand.red` = `#F84464`) — not rebuilding the client per the Phase 0 decision.
- Phase 0 setup (2026-07-08): DB for local build/verification = **MongoDB Atlas** (URI supplied by user, goes in server/.env — no local mongod on this machine). UI wiring scope for Phase 0 = **auth only** (login/signup/Google + role guards + real axios client in 0.5); all other client pages stay on mock data until their own phase. Chapter moderation default = APPROVED (§5). Client is already extensively built with mock data — do NOT rebuild it; integrate + wire per phase.
- Phase 0 task 0.1 (2026-07-08): the previously-committed root+server scaffold (commits 4e5635b, 9a06d24) had been deleted in the working tree; restored it (matches plan §13). Renamed the client workspace package `obs-events`→`client` (it collided with the root package name and broke `npm install` under workspaces). Consolidated the duplicate build-plan/progress files: `obs-events-build-plan new.md`/`PROGRESS new.md` were byte-identical to the unsuffixed files except CRLF line endings — removed the `*new*` copies and kept the unsuffixed names the plan/loop prompt reference. Added `.env` to .gitignore.
- Phase 0 task 0.5 (2026-07-08): "auth only" UI wiring per user decision — real axios client + AppContext auth + AuthModal + /account guards wired to the backend; all other client pages stay on mock data until their phase. Access token kept in memory only (not localStorage); session restored on load via silent /auth/refresh against the httpOnly cookie. Google button uses the official GIS rendered button (needs VITE_GOOGLE_CLIENT_ID; shows a disabled placeholder when unset). Kept legacy signIn/signOut in context for backward-compat with existing components. Guards live in components/guards/ (client has no layout components; App.jsx routes inline) — RequireRole ready for organizer/admin in Phase 1/3.
- Phase 0 task 0.4 (2026-07-08): mailer is provider-agnostic Nodemailer; with no SMTP_HOST it uses jsonTransport (serializes, does not deliver) so dev flows work without creds — swap in SMTP creds for real delivery. S3 client uses explicit env keys when present, else the SDK default credential chain (IAM role on EC2). Wired forgot-password to the mailer (the piece deferred from 0.3). slugify was already delivered in 0.2.
- Phase 0 task 0.3 (2026-07-08): refresh rotation backed by a new `Session` collection (jti per token) for real rotation + reuse detection — not in §5's list but required to implement the mandated rotation. Password reset is a stateless JWT bound to a fingerprint of the current passwordHash (single-use, no schema field). Strict 10/15min auth limiter applied only to credential routes (register/login/google/forgot/reset), NOT to /me or /refresh (those would break normal use; still covered by the global 100/15min). forgot-password logs the reset link to the console in dev — real SMTP send + EmailLog wired in 0.4. Generated dev JWT secrets into server/.env automatically. Google endpoint built + gated; full Google E2E deferred to 0.5 (needs user's GOOGLE_CLIENT_ID + browser flow).
- Phase 0 task 0.2 (2026-07-08): built §5 core models only; §5.1 standalone collections + §5.1 Event fields deferred to Phase 5; §5.1 Chapter fields (createdById/isOfficial/status) built now since the seed marks the 108 chapters isOfficial. Password hashing uses `bcryptjs` (pure JS, bcrypt-compatible, cost 12) not native `bcrypt`, for Windows portability. Added env-gated `dns.setServers()` (DNS_SERVERS) as a dev-only fix for Atlas SRV `ECONNREFUSED` on this network — no effect when unset (prod).
- v1.1: MERN stack; order items + invoice embedded in orders; all money as integer paise; ecosystemTier A–E added to chapters
- v1.3: email via Nodemailer (SMTP), provider-agnostic mailer util (env: SMTP_HOST/PORT/SECURE/USER/PASS + EMAIL_FROM) — replaces SendGrid; Node.js + MongoDB unchanged. chapter creation OPEN to any signed-in user (events stay organizer-gated) [CONFIRM: source said both — using chapters-open/events-gated]; new collections Sponsor, PartnerApplication, Speaker, Article, Program, ProgramDay; event fields ownership(OBS/Partner)/isLaunch/launchAt/programId/programDayNumber/speakerIds; chapter fields createdById/isOfficial/status; 100 Days = 15 Oct→22 Jan yearly; frontend prompt obs-frontend-new-sections-ui-prompt.md
- v1.2: explicit order+payment state model (§8.0, PENDING = draft/held); Google Maps Platform for venue autocomplete + geocode fallback + embedded map (§8.7); new keys GOOGLE_MAPS_API_KEY (server) + VITE_GOOGLE_MAPS_API_KEY (browser)

## Known issues / TODO
- **Git interleaving (2026-07-09)**: during Phase 2 the working tree was committed by another actor (user/IDE/hook) mid-task with non-"phase2:" messages (e.g. `0e5e67b "feat: integrate Stripe payment…"`, `9ac02dc`), so some task code landed in those commits and my `phase2: task 2.x` commit for that task only carried PROGRESS.md. All task code IS committed and the tree is coherent; one throwaway `verify-2.6.mjs` got captured and was removed in a follow-up commit. Heads-up: per-task commits may be split across these interleaved commits.
- Atlas is connected (URI in gitignored `server/.env`, db name `obs`). Dev needs `DNS_SERVERS=8.8.8.8,1.1.1.1` in `server/.env` for SRV lookups on this network; production (EC2) won't.
- Phase 0 EXIT ✅ PASSED (2026-07-08): user confirmed browser sign-in — email signup/login/logout via the AuthModal and Google (Continue with Google) both work; refresh rotation + seeds already verified. Phase 0 done. Next: await user go-ahead to start Phase 1 (organizer apply → admin approve; events CRUD/wizard; public catalog).
- `npm install` reports 2 transitive dev-dep vulnerabilities (vite/esbuild chain) — deferred; fixing needs a breaking major bump, out of Phase 0 scope.
- Node here is v24 (plan targets Node 20); satisfies `engines: >=20`. No action needed.
- Client `/t/:status` route param vs plan's `/t/:token` — cosmetic mismatch to reconcile when the validation page is wired (Phase 2.8).

## Session log
- 2026-07-08 · task 0.1 (repo scaffold) — DONE
  - Files: `package.json` (root workspaces, restored), `server/package.json`, `server/.env.example`, `server/src/app.js`, `server/src/config/env.js`, `server/src/index.js` (restored); `client/package.json` (renamed pkg → `client`); `.gitignore` (+`.env`); removed `obs-events-build-plan new.md`, `PROGRESS new.md`.
  - Verified: `npm install` resolves all 3 workspaces (236 pkgs); `node server/src/index.js` boots and `GET /api/v1/health` returns `{ok:true,...}`; `npm run build --workspace client` compiles (104 modules, vite 5.4).
  - Learned: client is a BrowserRouter SPA (main.jsx → BrowserRouter+AppProvider→App) with ~30 pages driven by `src/mock/*` + `AppContext`; no backend calls yet. Server index.js does NOT connect Mongo yet (that's 0.2), so it boots standalone.
- 2026-07-08 · task 0.2 (models + seed) — DONE
  - Built: `server/src/constants.js` (all §5 + §5.1 enums); 16 §5 models + `models/index.js` barrel; `config/db.js` (mongoose connect + dev DNS override); `utils/slugify.js`; `seed/chapters.data.js` (108-chapter builder) + `seed/seed.js` (idempotent: 12 categories, 108 chapters, 3 CMS stubs, admin). Wired `index.js` to connect Mongo on boot; extended `env.js` (SEED_ADMIN_*, DNS_SERVERS); added `bcryptjs` + `seed` script to server/package.json; documented DNS_SERVERS in .env.example.
  - Offline-verified: chapters build = exactly 108 (54 country incl. T1–T5×10 + Growth×4, 4 city, 50 thematic), unique slugs, flagship=16, ecosystemTier E=58/A=5/B=5/C=9/D=4/null=27; all 16 models compile; seed passes `node --check`.
  - LIVE-verified against Atlas (db `obs`): `npm run seed --workspace server` → 12 categories, 108 chapters, 3 CMS pages, 1 admin. Independent mongosh count matches: categories 12, chapters 108 (BUSINESS_CAPITAL 3 / GEO_CITY 4 / GEO_COUNTRY 54 / INDUSTRY_PROFESSIONAL 8 / LEADERSHIP_COMMUNITY 7 / STRATEGIC_EXPANSION 32), flagship 16, official 108, admin sahatushankar234@gmail.com. **0.2 exit check PASSED.**
  - Learned: Node's c-ares SRV lookup for the Atlas `mongodb+srv://` URI failed with ECONNREFUSED on this network (router refuses TCP DNS on :53) though nslookup worked; fixed with env-gated `dns.setServers()` (DNS_SERVERS=8.8.8.8,1.1.1.1 in dev .env). Connected DB name is `obs` (from the user's URI), not `obs-events`.
  - Scope note: §5.1 standalone collections (Sponsor/Speaker/Article/Program/ProgramDay/PartnerApplication) + §5.1 Event fields deferred to Phase 5; only the §5.1 Chapter fields (createdById/isOfficial/status) built now (seed needs isOfficial). Used bcryptjs (pure-JS, bcrypt-compatible at cost 12) instead of native bcrypt for Windows portability.
- 2026-07-08 · task 0.3 (auth module) — DONE
  - Built: `utils/{errors,tokens,asyncHandler}.js`; `models/Session.js` (+ barrel); `middleware/{errorHandler,requireAuth,requireRole,validate,rateLimit}.js`; `modules/auth/{auth.schemas,auth.service,auth.controller,auth.routes}.js`. Wired `app.js` (cookie-parser, trust proxy, global limiter, /api/v1/auth, notFound + typed errorHandler). Extended `env.js` (JWT secrets/TTLs, GOOGLE_*, REFRESH_COOKIE_NAME). Deps: jsonwebtoken, google-auth-library, zod, cookie-parser, express-rate-limit.
  - Endpoints: POST /auth/register|login|google|refresh|logout|forgot-password|reset-password, GET /auth/me. Access JWT 15m (Bearer), refresh JWT 30d in httpOnly cookie `obs_rt` (path /api/v1/auth), rotated per refresh with reuse detection.
  - LIVE-verified against Atlas (29/29 E2E checks via curl): register 201 + cookie, /me 200, /me-no-token 401 NO_TOKEN, dupe 409 EMAIL_TAKEN, bad input 400 VALIDATION_ERROR, login 200, wrong pw 401 INVALID_CREDENTIALS, **refresh rotation R1≠R2**, **reuse → 401 TOKEN_REUSE**, forgot 200 + reset 200 + login-new-pw 200 + reset-token single-use 400, logout 200 → refresh-after-logout 401, google 400 GOOGLE_NOT_CONFIGURED. Rate limiter verified separately (AUTH_RATE_LIMIT_MAX=3 → 4th/5th login = 429 RATE_LIMITED, health stays 200).
  - PENDING for Phase 0 EXIT: Google signup E2E (needs GOOGLE_CLIENT_ID + client button, 0.5). Windows note: stopping the npm task can orphan the child `node` (EADDRINUSE on restart) — kill the :4000 PID via `taskkill //F //PID <pid>` if a restart fails.
- 2026-07-08 · task 0.4 (utils) — DONE
  - Built: `config/s3.js` + `utils/s3.js` (presignPut/presignGet via @aws-sdk/s3-request-presigner); `config/mailer.js` + `utils/mailer.js` (Nodemailer transport w/ jsonTransport dev fallback, sendMail writes EmailLog QUEUED→SENT/FAILED); `utils/counters.js` (nextSeq + order/ticket/invoice formatters). Extended `env.js` (AWS_* + SMTP_* + EMAIL_FROM); deps @aws-sdk/client-s3, @aws-sdk/s3-request-presigner, nodemailer. Wired forgot-password → sendMail (PASSWORD_RESET). slugify already shipped in 0.2.
  - Verified (11/11 against Atlas): nextSeq atomic 1→2→3 + independent counters; formatters OBS-2026-000001 / OBS-TKT-000001 / OBS-INV-2026-000001; sendMail returns info + writes/queries an EmailLog SENT row; presignPut/presignGet return https X-Amz-Signature URLs with correct key + 300s expiry (dummy creds, local signing). Live: POST /auth/forgot-password → 200, EmailLog PASSWORD_RESET 0→1, mailer ran.
- 2026-07-08 · task 0.5 (client base) — DONE (code); browser + Google E2E pending
  - Built: `client/src/lib/api.js` (axios, in-memory Bearer + withCredentials + silent-refresh-on-401 interceptor), `lib/googleSignIn.js` (GIS loader), `components/common/GoogleButton.jsx`, `components/guards/RequireAuth.jsx` (+ RequireRole). Extended `context/AppContext.jsx` (login/register/loginWithGoogle/logout + on-load silent refresh + authReady). Rewrote `components/layout/AuthModal.jsx` to call the real API + GIS. Wrapped `/account/*` routes with RequireAuth in `App.jsx`. Added axios dep; created `client/.env(.example)` (VITE_API_URL, VITE_GOOGLE_CLIENT_ID).
  - Verified: client builds (vite, 160 modules); client-behavior E2E (Node sim of api.js against live backend) 8/8 — register 201+cookie, /me 200, expired-token→SILENT REFRESH→/me 200, cookie rotated, logout 200, post-logout blocked; full stack runs together (backend /health 200 + Vite dev shell 200, CORS origin matches APP_URL).
  - Google follow-up (2026-07-08): user set GOOGLE_CLIENT_ID in server/.env; copied the (public) client id into client/.env as VITE_GOOGLE_CLIENT_ID (they must match — GIS audience). Restarted server → /auth/google now returns GOOGLE_INVALID for a bogus token (configured + verifying). Remaining EXIT step is the human browser sign-in (email + Google consent) — not automatable.
- 2026-07-08 · task 1.1 (organizer apply → admin approve/reject + emails) — DONE
  - Server built: `modules/organizers/{schemas,service,controller,routes}.js` (POST /organizer/apply, GET /organizer/me — both requireAuth); `modules/admin/{schemas,service,controller,routes}.js` (GET /admin/organizers?status, POST /admin/organizers/:id/approve|reject — requireAuth + requireRole ADMIN); `utils/audit.js` (writeAudit → AuditLog); `utils/slugify.js` +`uniqueSlug(Model, source)` helper (reused by events in 1.2). Wired both routers in `app.js` (/api/v1/organizer, /api/v1/admin). Emails ORGANIZER_APPROVED / ORGANIZER_REJECTED via existing mailer (writes EmailLog); mail send is best-effort (never blocks the action).
  - Client wired: NEW `components/portal/{Kit,PortalShell,AdminLayout}.jsx`; `lib/api.js` +domain methods (attached to axios, unwrap payload) + `apiErrorCode()`; rewrote `pages/organizer/Apply.jsx` (fetches /organizer/me → shows PENDING/APPROVED status or the form; REJECTED can re-apply) and `pages/admin/Organizers.jsx` (status tabs + real approve/reject with reason prompt); routed `/organizer/apply` (RequireAuth) + `/admin` + `/admin/organizers` (RequireRole ADMIN) in App.jsx; `/list-your-event` CTAs → `/organizer/apply`.
  - Verified: in-process HTTP E2E against Atlas **26/26** — register→apply(201 PENDING)→me→dup(409 APPLICATION_PENDING)→validation(400)→unauth(401); admin login→non-admin(403)/unauth(401)→PENDING list incl. applicant w/ email→approve(200 APPROVED)→**role promoted to ORGANIZER** (DB + re-login token)→me APPROVED→AuditLog+EmailLog(SENT) written; reject path (200 REJECTED, role stays USER, EmailLog+AuditLog reason)→re-apply after reject (PENDING, same row). Cleanup removes all test docs. Client `npm run build` clean (165 modules). Full browser click-through deferred to the Phase 1 EXIT demo (human).
  - Learned: `.populate('userId','name email')` omits `role`, so the first approve run left role=USER — fixed by promoting via a conditional `updateOne` filter instead of reading the populated doc's role. SMTP is live (Gmail) in this env, so approve/reject actually send; test users use `@example.com` (reserved, bounces harmlessly). `obs-home-ui-prompt.md` (M) + `obs-inner-pages-ui-prompt.md` (??) were already in the tree at session start — left them out of the 1.1 commit.
- 2026-07-08 · task 1.2 (organizer event CRUD in DRAFT + banner presign + ownership guards) — DONE (backend only; wizard UI is 1.3)
  - Built: `modules/events/{events.schemas,events.service,events.controller,events.organizer.routes}.js` (GET/POST /organizer/events, GET/PATCH/DELETE /organizer/events/:id, POST /organizer/events/:id/banner); `middleware/requireApprovedOrganizer.js` (gates on APPROVED profile → req.organizer); `utils/s3.js` +`objectUrl()`. Relaxed Event model requireds (categoryId/description/startAt/endAt now optional; title+slug still required). Mounted `/api/v1/organizer/events` in app.js (before `/api/v1/organizer` so the more-specific prefix wins).
  - Verified: in-process HTTP E2E against Atlas **30/30** — guards (non-organizer 403 NOT_ORGANIZER, unauth 401); create draft (title-only → 201 DRAFT, slug `summit-of-legends`), duplicate-title unique slug (`-2`), short-title 400; list + pagination fields; get-own; PATCH full step-3 data w/ populated category+chapter; INVALID_DATE_RANGE 400; unknown categoryId → INVALID_CATEGORY 400; banner presign (uploadUrl+key+fileUrl, key `banners/<id>/<uuid>.png`, bad type 400); ownership (B gets 403 on A's event for GET/PATCH/DELETE, B's list empty); state guards (force PENDING → PATCH/DELETE 409); edit REJECTED → back to DRAFT + reason cleared; delete DRAFT → 200 then 404. Presign tested with dummy AWS creds (local signing, no network). All server files node --check clean.
  - Learned: presign needs *some* AWS creds present to sign (dummy is fine locally — it never contacts AWS). Draft-first required relaxing the model; kept a service-level `endAt > startAt` check whenever both are present (full required-set enforced at submit in 1.4).
- 2026-07-08 · task 1.3 (6-step wizard + Google Places/geocode) — DONE
  - Server: `modules/geo` (POST /geo/geocode, requireAuth → live Google Geocoding via server key), `modules/categories` (GET /categories), `modules/chapters` (GET /chapters + ?type&tier); env +GOOGLE_MAPS_API_KEY; mounted /api/v1/{categories,chapters,geo}.
  - Client: `lib/googleMaps.js` (Places loader, no-op without VITE key), `lib/api.js` +catalog/geo/organizer-event methods + `uploadToPresignedUrl`, `components/portal/OrganizerLayout.jsx` (gates on APPROVED profile), rewrote `pages/organizer/Events.jsx` (real list + delete drafts) and `pages/organizer/EventWizard.jsx` (create+edit, per-step save, banner upload, Places→manual+geocode, locked steps 4–5, review); routed `/organizer`, `/organizer/events`, `/organizer/events/new`, `/organizer/events/:id/edit` under RequireAuth+OrganizerLayout; Header account menu role links.
  - Verified: backend E2E **11/11** (categories 12, chapters 108 + type filter, geocode live 200 + 404 no-results + 401 unauth); wizard-sequence E2E **7/7** (create → geocode-fallback + venue patch → banner presign+persist → complete DRAFT with category/schedule/pin/banner). Client `npm run build` clean (169 modules). Browser click-through = Phase 1 EXIT (human).
  - Learned: server maps key present (geocode works live); client maps key empty → wizard uses manual entry + server geocode, which is the tested path. Bash cwd resets to repo root between calls — run verify scripts with `cd server && …`.
- 2026-07-08 · task 1.4 (submit → PENDING_APPROVAL; admin event queue; state machine) — DONE
  - Server: `events.service` +submitEvent + assertSubmittable (422 EVENT_INCOMPLETE w/ missing[]) + POST /organizer/events/:id/submit; `admin` module +listEvents/approveEvent/rejectEvent (GET /admin/events?status, POST /admin/events/:id/approve|reject) — approve→PUBLISHED+publishedAt, reject→REJECTED+reason (required), both write AuditLog + email organizer (EVENT_APPROVED/REJECTED); all transitions guarded in the service (409 on invalid).
  - Client: `api.js` +organizerSubmitEvent/adminEvents/approveEvent/rejectEvent; wizard step 6 Submit-for-approval (handles REJECTED→DRAFT + 422 missing toast); rewrote `pages/admin/Events.jsx` (status tabs + approve/reject-with-reason), added to AdminLayout nav + routed `/admin/events`.
  - Verified: state-machine E2E **20/20** — submit-incomplete 422 (+missing list) → complete → ownership 403 → submit 200 PENDING → re-submit 409 → edit-pending 409 → admin queue shows it → reject-no-reason 400 → reject 200 REJECTED(+reason) → approve-rejected 409 → EmailLog/AuditLog EVENT_REJECTED → edit→DRAFT(reason cleared) → resubmit PENDING → approve 200 PUBLISHED(+publishedAt) → EmailLog/AuditLog EVENT_APPROVED → DB PUBLISHED. Client build clean.
- 2026-07-08 · task 1.5 (public listing API + listing page) — DONE
  - Server: `events.service` +listPublicEvents + publicEventCard (lean shape) + escapeRegex; `events.public.routes.js` (GET /events) mounted at /api/v1/events; publicListQuery + slugParam schemas.
  - Client: `api.listEvents`; NEW `components/common/ApiEventCard.jsx`; rewrote `pages/EventsListing.jsx` (API-driven filter rail: date presets / category / chapter / mode; sort dropdown; applied chips; load-more; mobile sheet — dropped owner tabs + price).
  - Verified: public-list E2E **13/13** — published-only (draft hidden), city/mode/category/chapter filters, unknown-slug empty, q search, sort=soonest ordering, pagination (limit/page/total/pages), dateTo range. Created events via the real submit→approve flow then cleaned up. Client build clean.
- 2026-07-08 · task 1.6 (event details + public pages) + Phase 1 EXIT — DONE
  - Server: `optionalAuth` middleware; events getPublicEventBySlug (+viewsCount, meetingLink hidden) + similarEvents + GET /events/:slug(/similar); organizers getPublicProfile + GET /api/v1/organizers/:slug; chapters getChapterBySlug + join/leave (GET /chapters/:slug, POST|DELETE /chapters/:id/join); seed +demo organizer + 3 published events.
  - Client: react-helmet-async + HelmetProvider + `Seo` component; api detail/organizer/chapter/join methods; rewrote EventDetail (disabled booking card, keyless embedded map + directions, WhatsApp/X/LinkedIn/copy share, similar, helmet), Organizer, Chapters (grouped directory + search), ChapterDetail (join/leave, About), Home (real events/categories/chapters; Phase-5 rails dropped), SearchResults (real events + chapters).
  - Verified: detail E2E **16/16** (detail+viewsCount, similar, organizer, chapter+join/leave idempotent/auth). **Phase 1 EXIT E2E 17/17**: logged-out browse/filter/detail/chapters/organizer over seeded data + fresh organizer draft→wizard→submit→admin-approve→appears on public listing (pending event 404s publicly). Client build clean (166 modules). Booking rendered-but-disabled held throughout; Phase 2 not started.
- 2026-07-09 · task 2.1 (ticket types + promo codes CRUD; wizard steps 4–5 live) — DONE
  - Server: `modules/ticketTypes/*` + `modules/promoCodes/*` (GET/POST/PATCH/DELETE under /organizer/events/:eventId/{ticket-types,promo-codes}); exported `loadOwnedEvent` from events.service for the ownership guard; mounted the two mergeParams routers in app.js before /organizer/events.
  - Client: `lib/api.js` +8 CRUD methods; NEW `components/organizer/{TicketTypesEditor,PromoCodesEditor}.jsx`; wizard steps 4–5 now render the live editors (₹↔paise conversion) instead of the locked placeholder; review-step note updated.
  - Verified: in-process E2E **21/21** — ticket types (create paise/defaults, list, update, max<min 400, negative price 400, quantityTotal<sold 400, delete-with-sales 409 / no-sales 200, ownership 403, unauth 401); promo codes (PERCENT/FLAT create, code uppercased, PERCENT>100 400, duplicate 409, update, delete-used 409 / unused 200, ownership 403). `SMTP_HOST=` → jsonTransport (no real sends). Client build clean.
- 2026-07-09 · task 2.2 (POST /orders atomic reserve + expiry cron + cancel) — DONE (backend only; checkout UI is 2.6)
  - Server: `modules/orders/*` (POST /orders, POST /orders/:id/cancel, GET /me/orders(+/:id)); `modules/fulfillment/fulfillment.service.js` (`markPaidAndFulfil` core — flip+tickets+email, shared with 2.3/2.4, enriched in 2.5); `jobs/expireOrders.js` + node-cron `*/5` in index.js; env += SERVICE_FEE_PERCENT/ORDER_HOLD_MINUTES/REFUND_CUTOFF_HOURS + RAZORPAY_*/STRIPE_* placeholders; mounted /api/v1/orders + /api/v1/me/orders. Deps: node-cron.
  - Verified: in-process E2E **34/34** — money exact (subtotal/fee/total; SAVE10 10%, FLAT50 ₹50+minorder, PROMO_MIN_ORDER, PROMO_INVALID), item price snapshots, `$expr` SOLD_OUT (no quantitySold change) + **multi-item txn rollback** (earlier item's reserve rolled back), free order → PAID/FREE + 1 VALID ticket + REGISTRATION_CONFIRMATION, 100%-promo→free + usedCount++, cancel (owner/403, restore, 409 re-cancel), expiry cron (EXPIRED + restore + idempotent), /me/orders list+detail+ownership, unauth 401, DRAFT-event 400, maxPerOrder 400. Ran with `SMTP_HOST=` (jsonTransport). Transactions run live against Atlas (replica set). Client checkout UI = 2.6.
- 2026-07-09 · task 2.3 (Razorpay order/verify/webhook) — DONE (backend; checkout UI is 2.6)
  - Server: `config/razorpay.js`; `modules/payments/{payments.shared,razorpay.service,payments.schemas,payments.controller,payments.routes,webhooks.controller,webhooks.routes}.js`; app.js mounts `/api/v1/webhooks` with `express.raw()` BEFORE `express.json()` + `/api/v1/payments`. Dep: razorpay.
  - Verified: in-process E2E **19/19** — order-create guards (401/403/503-not-configured); verify valid→200 processing + **does NOT fulfil** (order PENDING, 0 tickets), bad sig→400; webhook captured→PAID+paidAt+Payment CAPTURED+2 VALID tickets+PAYMENT_SUCCESS+TICKET_DELIVERY, re-delivery→alreadyPaid (no double fulfil), bad sig→400, amount-mismatch→ignored (PENDING), payment.failed→FAILED+inventory released+Payment FAILED. Dummy secrets + locally-signed raw payloads.
- 2026-07-09 · task 2.4 (Stripe intent/webhook) — DONE (backend; checkout UI is 2.6)
  - Server: `config/stripe.js`; `modules/payments/stripe.service.js` (createStripeIntent + handleStripeWebhook); wired stripeIntent into payments.controller/routes (`POST /payments/stripe/intent`) + stripe_ into webhooks.controller/routes (`POST /webhooks/stripe`). Dep: stripe.
  - Verified: in-process E2E **13/13** — intent guards (401/403/503); webhook succeeded→PAID+gateway STRIPE+Payment CAPTURED+2 VALID tickets+PAYMENT_SUCCESS+TICKET_DELIVERY, re-delivery→alreadyPaid (no double fulfil), bad sig→400, amount-mismatch→ignored (PENDING), payment_failed→FAILED+inventory released. Signed via SDK `generateTestHeaderString` + dummy `STRIPE_WEBHOOK_SECRET`.
- 2026-07-09 · task 2.5 (fulfilment: tickets, QR, PDF, invoice, S3, emails) — DONE (backend; ticket UI is 2.7)
  - Server: `utils/qr.js` (qrPng), `utils/pdf.js` (buildTicketPdf + buildInvoicePdf, pdfkit→Buffer), `utils/s3.js` +putObject + isS3Configured, `utils/mailer.js` +attachments; enriched `fulfillment.service.markPaidAndFulfil` (QR+PDF per ticket→S3 best-effort + attachments; invoice PDF paid-only→embedded order.invoice; 8 MB attachment cap → links). Deps: qrcode, pdfkit.
  - Verified: in-process E2E **12/12** — generators emit valid PNG/PDF buffers; captured webhook → order PAID + invoice `OBS-INV-2026-000001` (+issuedAt) + 2 tickets (`OBS-TKT-` + qrToken) + PAYMENT_SUCCESS/TICKET_DELIVERY SENT; free order → PAID, **no invoice**, 1 ticket + REGISTRATION_CONFIRMATION. pdfUrl null in dev (S3 skipped, no creds). `SMTP_HOST=` jsonTransport.
- 2026-07-09 · task 2.6 (checkout + success + live booking card) — DONE
  - Server: `events.service` — public event detail now includes active `ticketTypes[]` (with `onSale`) + `serviceFeePercent`.
  - Client: `lib/api.js` +order/payment methods; `lib/loadScript.js`; NEW `components/booking/BookingCard.jsx` (steppers/promo/estimate → createOrder → checkout|success) + `components/checkout/StripePaymentForm.jsx` (Payment Element); rewrote `pages/{Checkout,Success}.jsx` (real order by :orderId, countdown, gateway selector, Razorpay checkout.js + Stripe Elements, success polling); EventDetail booking card is live; routes `/checkout/:orderId(+/success)` behind RequireAuth. Dep: @stripe/stripe-js.
  - Verified: backend contract E2E **6/6** (active ticketTypes only + fee on detail; order detail shape for checkout; free booking → PAID). Client build clean. Gateway pay modals = EXIT (keys + browser).
- 2026-07-09 · task 2.7 (my tickets, ticket detail QR/PDF/.ics, order history) — DONE
  - Server: `modules/tickets/*` (GET /me/tickets?scope, /me/tickets/:id (+qrDataUrl), /me/tickets/:id/pdf stream); `utils/qr.js` +qrDataUrl; mounted /api/v1/me/tickets.
  - Client: `lib/api.js` +myTickets/myTicket/ticketPdfBlob; `lib/ics.js`; rewrote `MyTickets` (tabs), `TicketDetail` (QR img + PDF blob download + .ics), `Orders` (real history → checkout/success).
  - Verified: E2E **8/8** — list scope upcoming/past, ownership-empty, unauth 401; detail qrDataUrl+validationUrl + non-owner 403; PDF stream `application/pdf` `%PDF` + unauth 401. Client build clean.
- 2026-07-09 · task 2.8 (public validation page /t/:token) — DONE
  - Server: `modules/tickets/tickets.public.routes.js` + validateByToken (masked name); mounted public `/api/v1/tickets/validate/:qrToken`.
  - Client: `api.validateTicket`; rewrote `Validate` (fetch by token → VALID/USED/CANCELLED/not-found); route `/t/:status`→`/t/:token`.
  - Verified: E2E **6/6** — VALID (public, event+venue+masked name+ticketNumber), USED (+checkedInAt), CANCELLED, unknown token 404. Client build clean.
